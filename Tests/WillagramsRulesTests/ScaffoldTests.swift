import Testing
@testable import WillagramsRules

// Proves the package builds and the test runner is wired up.
// Foundation items 2-7 add the real fixtures alongside this.
@Test func packageBuilds() {
    #expect(Bool(true))
}
