# Willagrams — polish / final lane progress

LANE.md is the contract; this tracks where we are in it. If they disagree, LANE.md wins for scope.

## Current position

- **Status:** round 3 (2026-09-15) — the hand-test round. Nate installed the build on his real iPhone 13 mini and iPad Air and played it for the first time; he found eight functional bugs and four visual breakages. Round 2's shutdown sequence has now been run and closed out (see below), so `lane/final` is at `4fa9b70` and `auto/final` is level with it. Twelve round-3 items are written into `LANE.md`, none started
- **Round 2's shutdown sequence — run 2026-09-15, and its "suite green" line was WRONG:** `xcodebuild` BUILD SUCCEEDED and `scripts/scratch-verify.sh` was green (6 migrations, 55 assertions, 0 leftover rows), both stand. But the full-suite check read each package's *collected count* rather than its result, so `Tests/ShellTests` was recorded as "264, green" when it was in fact 264 collected with 4 failing. Corrected 2026-09-16 by re-running the suite serially and in isolation at load 8, and by reproducing the identical 4 failures at the merge base `4fa9b70` itself. They predate round 3 and are now round-3 item 5. Lane acceptance `dt-review` returned MET on all three round-2 criteria, but criterion 2 rested on that same false green, so it is **not** met as recorded; `auto/final` merged into `lane/final` as a clean fast-forward to `4fa9b70`
- **A reproducible iPad launch crash was found after that merge, and it changes round 2's criterion 1 from met to UNMET on the iPad half.** A cold launch on an iPad Air 11-inch (M4) Simulator writes a crash report within ~15s, twice out of two attempts (`Willagrams-2026-09-15-225441.ips`, `-225705.ips`). `EXC_BREAKPOINT`/SIGTRAP on `com.apple.root.default-qos`; the faulting frame is the orientation `requestGeometryUpdate` error handler in `ShellRootView.swift` (~70-75), which is `@MainActor`-isolated but called by UIKit on a background queue. iPhone SE is unaffected. **This is an App Store blocker and is round 3's item 1**
- **Next:** the newly-inserted item "a shipped match can tell that a peer is gone", then items 12-14 in `LANE.md` order. Items 1-10 are done
- **A pre-existing Critical was found by item 10 and is now its own item, inserted before "a dead match says so".** Nothing in round 3 caused it; `git log -L` puts it at `7de2d97`, the original façade commit. Two different objects subscribe to the same single-subscription peer-presence stream, and handing over from the first cancels it, which terminates the stream for the second. So on every match the app actually builds, the part that notices your opponent left never receives anything. Item 10's work is real and correct, but half of it — being TOLD a peer is gone — is inert in production until this is fixed, and "a dead match says so" is built directly on top of it, which is why it goes first. Nate: this is why locking your own phone is fixed but a match still may not notice the OTHER player vanishing
- **Per-item floors now (counts are floors):** rules 53 · Board 271 · Match 128 · Style 44 · Shell 291 (run `--no-parallel`) · Settings 37 · Bot 68 · Online 168 (1 known pre-existing issue at `WholeMatchScript.swift:98`) · Audio 19 · Account 21 · Friends 54
- **Owed to Nate, carried from round 2, none blocking:** item 2's `elapsedIsMeasuredFromPlaying` driver change wants sign-off; item 6's text self-conflicts on the Join button (code follows "disabled only when empty or in flight"); item 8 left a white system launch-screen flash its `done when:` did not cover
- **Round-3 scope decisions Nate made:** invites stay live-only broadcasts, so no new table, no migration and no production SQL — a friend with the app closed misses the invite, accepted. The lock-screen bug is not a crash: the app stays responsive and only Draw and Swap die. "PvP special match rules" is dropped — solo and Play a Friend already render the same options and he had not found the gear
- **Blockers:** none open. Three round-2 follow-ups are deliberately out of scope this round and live in `LANE.md`'s `## Out of scope`: app-wide Dynamic Type, the white launch flash (needs the protected `project.pbxproj`), and the `HostLobbyLayout.swift` orphan
- **Standing constraints:** migration `0006` is live (Nate applied it 2026-09-15: 40 of 1,274 fastest wins cleared, null guard present). Never `supabase db push` here — the live migration history is empty. `Config/Secrets.local.xcconfig` is gitignored and missing from 29 of 32 worktrees; a device build from one of them silently ships an empty Supabase key and every sign-in fails. `.git/info/exclude` now protects it and `.env` in every worktree
- **Open coverage debt from item 9, carried deliberately:** the `declineSends` task bookkeeping in `ShellModel.swift:460` is not pinned by value — mutations N1/N2/N3 survive. It is a coverage gap, not a known defect; the decline path's behaviour is pinned, only the dictionary's insert/remove is not. The in-flight pass that tried to close it still had a mutation applied to the tree and was never gated, so it was discarded; its patch sits at `~/.claude/jobs/782ad301/tmp/item9/ungated-inflight-v2.patch` for whoever finishes it — use `-v2`, which is the builder's own redo and leaves the `isAcceptedFriend` guard intact; the earlier `ungated-inflight.patch` is kept only as the evidence that v1 had mutation MQ2 still applied
- **Known ceiling from item 9, accepted:** a decline is one unacknowledged live broadcast, so a host whose app is closed at that moment never learns of it and the open seat keeps waiting — the same live-only limitation Nate accepted for invites themselves
- **Second known ceiling from item 8, accepted:** the pin covers the chain from the app's route switch down to the button, but not the one link above it — hiding the whole app's root view at `WillagramsApp.swift:42` still leaves the Shell suite green. That link is covered instead by direct evidence: the cold-launch runs on both an iPhone and an iPad Simulator show Home actually rendered
- **Known ceiling from item 8, accepted:** the invite picker is pinned by matching eight whole declarations of source text from `ShellRootView`'s route arm down to the button, so any legitimate future edit to any of them false-reds the test and whoever makes it re-extends the literal. A rename applied consistently to both the screen and the test, or a change inside a type the test only names, stays invisible — only a Simulator UI test would catch those, and there isn't one
- **Known ceiling from item 7, accepted:** `JoinTests.swift:540` pins the join status through its arm's closing brace, so adding a legitimate sibling view next to `joinStatus(join)` will false-red it — extend the literal when that happens. That brittleness is the price of a pin that can tell "rendered unconditionally" from "present in the file". Also unobserved live: `resolveHostName` is a separate async read, so on a slow network the nameless "Waiting for host" shows first and the name lands after
- **Open MINORs carried from item 6, none blocking:** `ShellModel.swift:356` binds self strongly before an `await` inside a `[weak self]` task, so a dropped shell is held alive for the round trip; nothing cancels `profileRefreshTask` at `:355`, so repeated open/close of Profile issues unbounded concurrent reads (resource only, correctness is pinned by a stamp); `MatchOutcomeRecorder.swift:263` still discards its returned row and is safe only because `showProfile()` re-reads — an invisible coupling that wants a comment
- **Open MINOR carried from item 5:** `JoinTests.swift:139` stays non-vacuous only while the registration never fires before `returnToMenu()`; it wants `#expect(model.phase == .waiting)` pinned. Item 7 touches `JoinModel` and should fold it in
- **Last updated:** 2026-09-16

## Round 3 — hand-test fixes

| Item | Status |
|------|--------|
| iPad launch crash | done — The app no longer dies on an iPad a few seconds after opening. The message iOS sends back when it refuses a rotation used to be handled on the wrong thread, which killed the app; it is now handled safely wherever it arrives. Three cold iPad launches and an iPhone launch came up clean. Still needs your eyes: that the iPad actually turns landscape, which nothing here can tap or rotate to check. (2026-09-15) |
| Buttons and codes stop wrapping | done — Button labels no longer break mid-word on an upright phone: they now shrink slightly instead of wrapping, and the smaller phone font finally applies upright as well as sideways. Friend codes are pinned to one line at full size and can never shrink or get cut off. One fix covered all four things you photographed — `Cop`/`y`, the giant bars on Friends, the wrapped codes, and `Standa`/`rd` in Solo. Still needs your eyes: those screens only open by tapping. (2026-09-16) |
| Friend row fits a phone | done — A friend now takes one tidy row: the main action (Accept, or Invite to play) stays on the row as a button, and Decline, Block and Unfriend moved into a "More actions" menu behind a dots icon. Nothing was dropped — all five still work, and Unfriend still asks first. The three giant bars are gone. Still needs your eyes: Friends only opens by tapping. (2026-09-16) |
| Solo stops saying HOST | done — Solo Practice no longer carries a "HOST" tag at the top of its options card. The two-player lobby keeps its own "TWO PLAYER" tag, which turned out to be separate code — so nothing was lost there. Still needs your eyes: both screens only open by tapping. (2026-09-16) |
| The Shell suite tells the truth | done — The Shell test suite had four failures that round 2 recorded as green, and chasing them found a real bug in the shipped app: a match kept itself alive forever after it ended, so a long session slowly ate memory and could crash on read-back. That is fixed, along with the same mistake in the host lobby and the join screen. Two of the four failures were tests asserting old behaviour and were rewritten to the current rule, not deleted; one was the test file's own fake clock, which could only wake one sleeper while a solo match has two. The suite now runs 266 tests with zero failures, twice in a row. (2026-09-16) |
| A saved name reaches the whole app | done — Changing your name now sticks: the new name reaches the rest of the app instead of only the Profile screen, so leaving and reopening Profile shows it. Chasing it turned up a second bug of the same shape — your Played, Won, Tiles placed and Fastest win were frozen at whatever they were when you signed in, because finishing a match never handed the updated numbers back either. Both fixed. And on a build that can't reach the server, Save is now greyed out with a reason underneath instead of looking ready and quietly doing nothing. Still needs your eyes: rename and reopen Profile; finish an online match and check the four stats moved. (2026-09-16) |
| Joining says it worked | done — After you type a friend's code, the screen now shows a spinner while it connects and then "Waiting for host: <name>" once you're in, instead of sitting there silently and leaving you unsure whether it worked. The wording was already written and already tested — no screen was reading it, which is the whole reason this shipped broken. Two of the three attempts went into making the test actually able to fail: the first version stayed green even when the new line was hidden, or shown only to the host. Still needs your eyes: join a real game from a six-character code. (2026-09-16) |
| Invite a friend from the open seat | done — While you're waiting in a two-player lobby, the empty seat now has an Invite button that lists your friends, so you can pull someone in without leaving the screen and losing your code. Before this, an invite could only be sent from the Friends list — the one place you aren't when you're waiting for someone. Only accepted friends appear: nobody with a pending request, nobody you've blocked. The invite itself is the same one the Friends list sends, sharing one piece of code, so the two can't drift apart. The feature worked on the first try; all three attempts went into making the test able to fail, because a test that reads the screen's code can prove the button is written down without proving anyone can actually get to it — the earlier versions stayed green when the button was hidden, or shown only to the other player. Still needs your eyes: Play a Friend → Host, then check the Invite button on the dashed empty-seat row, that the list holds only real friends, and that tapping a name leaves your code on screen. (2026-09-16) |
| Decline a match invite | done — An invite you don't want now has a Decline next to Join. Tapping it clears the banner straight away and tells the host, whose empty seat then says who turned it down, so they stop waiting on someone who isn't coming. A decline is only ever that one message: it never sends an invite back, never blocks or unfriends, and can only go to someone who is already your friend. Review caught a real bug before this shipped: inviting the same person again right after they declined showed them nothing at all for two minutes, because the app was still hiding the old banner. Fixed, and the case is now tested. One limit, same as invites themselves: if the host's app is closed at that moment they never hear about it and the seat keeps waiting. Still needs your eyes: decline an invite and watch the banner go; check the host's seat names the right person; then invite that same friend again and confirm the banner comes back. (2026-09-16) |
| A screen lock no longer kills the match | done — Locking your phone mid-match no longer breaks it. Before, locking the screen suspended the app and dropped its connection, and two separate countdowns kept running while you were away, so by the time you unlocked, both had expired: everything still looked fine and responded, but Draw and Swap were dead with no message — exactly what you saw. Now the app notices when it goes off screen, both countdowns pause instead of burning down while you're away, and the connection is re-established before either starts counting again. Review caught a serious one before this shipped: a rounding error refunded a fraction of a second every time, so locking repeatedly could hold a genuinely-departed opponent as "reconnecting" forever — a match that could never end. Fixed. Still needs your eyes: lock a real device mid-match and check Draw and Swap BOTH work on resume; lock two or three times and confirm the reconnect countdown actually reaches its end rather than freezing; play a real two-device match across a screen lock. (2026-09-16) |
| A shipped match can tell that a peer is gone | not started — a pre-existing bug this item uncovered, see below |
| A dead match says so | not started |
| Double tap works on a letter | not started |
| Letters draw when they should | not started |

## Round 2 — final adjustments (shipped 2026-09-15)

| Item | Status |
|------|--------|
| Fast drag never flies home | done — A tile now lands in the cell it looks like it's over, and one dropped on a taken cell slides into a free cell next to it instead of flying home. (2026-09-15) |
| Resign wins skip fastest win | done — A win because your opponent resigned or left now counts as a win but never sets your fastest-win time; winning by playing out still records it. (2026-09-15) |
| iPhone portrait except gameplay | blocked — The phone now opens upright, but the iPad landscape check can't be proven from a simulator screenshot, and the upright Home is cut off until Home is rebuilt. (2026-09-15) |
| Tighter phone margins | done — Screens on an upright phone now sit 16pt from the edge instead of 40, so they stop looking cramped; iPad and sideways phone are unchanged. (2026-09-15) |
| Home rebuilt | done — Home now opens upright as a single column: the wordmark up top, then Multiplayer (marked coming soon), Play a Friend, and a tidy grid of Solo Practice, Profile, Friends and How to Play, with the old tagline gone. (2026-09-15) |
| One Play / Join a Friend screen | done — Play a Friend and Join a Friend are now one screen: two chips switch between them, the code shows as six tiles, and hosting still copies, shares and starts exactly as before. Switching chips now properly leaves the game you were in, which it previously did not. (2026-09-15) |
| Play a Friend match settings | done — Playing a friend now has its own settings: a gear on the host's screen opens starting tiles, shortest word, Swap and word list, and the guest's game is dealt with whatever the host chose instead of a fixed 21. The gear closes once you press Start. (2026-09-15) |
| Looping loading screen | done — The app now opens on the wordmark tiles flying together over a dark ground with a "Shuffling the Pool" caption, looping until sign-in and the dictionary are ready and then finishing its cycle before Home appears. One flaw left: the system's own first screen is still white for an instant before the dark one takes over. (2026-09-15) |
| Profile and Friends restyle | done — Profile and Friends now match the final design: an avatar card, stat cards with a win-rate bar, and friend rows as tiles, with Fastest win, Block and Unfriend all kept. (2026-09-15) |
| How to Play pager + Solo setup portrait | done — How to Play is now one rule per page with Back and Next and a Done on the last page, and Solo setup fits an upright phone in a single column. (2026-09-15) |

## Round 1 — phone polish (shipped 2026-09-15)

| Item | Status |
|------|--------|
| One sizing mechanism, proven on Menu | done — The home screen now fits a landscape phone without scrolling, with the logo centered and sized to the screen and smaller buttons on phones. |
| Apply sizing to remaining fixed-size screens | done — The lobby's invite code and buttons, the results and countdown cards, and solo setup all tighten up on a phone. |
| HUD bag legible on a phone | done — The bag in the match HUD is smaller on a phone and its count never truncates. |
| Typing screens keep the field visible | done — Profile, Friends and Join scroll the field you're typing in above the keyboard, and Join's button now sits beside its code field. |
| Reachable validation messages | done — A too-long name, a short friend code or a short join code now shows its message instead of silently greying out the button, and Return submits. |
| Unfriend | done — Each friend's row has an Unfriend button that asks first; unfriending never removes a block. |
| Zoom further out | done — You can pinch the board out further, to smaller tiles, so big boards fit on screen. |
| No drag snap-back | done — A tile you let go of stays where it lands, even on a fast drag; only an occupied cell sends it back. |
| Recenter frames every tile | done — Recenter now fits every tile on screen, clear of the bag and buttons, zooming out as far as needed. |
| No fly-in on pan | done — Tiles already on the board no longer fly in from the bag when you pan them back into view; new tiles still do. |
| Drawn tiles land near the board | done — Tiles from a Draw now land just below or beside your largest group of tiles. |
| Early start fix | done — An online match now starts only when the lobby creator presses Start. |
| Guest bag count | done — Both players now see how many tiles are left in the bag. |
| WILLA word + flourish | done — WILLA now counts as a word, and spelling it tints the tiles with a one-time sparkle. |
