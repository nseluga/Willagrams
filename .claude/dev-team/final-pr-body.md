# lane/final — team-memory entries (append to team-memory.md on integration)

## 2026-09-15 13:40 — dev-team-auto — final item 2: resign wins skip fastest win
- **Outcome:** DONE — 1 attempt — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high — auto/final-a2, 4bdb10a
- **What happened:** `recordOutcome` now takes `elapsedSeconds: Int?`. The recorder sends nil when `winningPlacements == nil` (only `depart` passes nil, which covers resign and abandon), `ProfileStats` mirrors SQL 0006, and `OutcomeParams` got a hand-written `encode(to:)` with `encodeNil`. QA and review ran in parallel on the checkpoint commit and both came back clean on the first pass.
- **What worked:** Separate mutations for the resign branch (M1), the claim-win guardrail (M2) and the explicit null (M3), each failing its own named assertion. Profiles seeded at 7/4/61 caught a blind write. Running the read-only review alongside QA saved about 5 minutes.
- **What failed:** `elapsedIsMeasuredFromPlaying` also depended on the old resign-win timing, and the item didn't name it; it was fixed by driving the test with `claimWin`. The first ShellTests run under load showed 25 issues that passed on re-run. dt-review wrote a repo-root `STANDARDS.md`, which I deleted as outside the lane's paths.
- **Remember next run:** Synthesized `Encodable` leaves out nil optionals, so any RPC params struct with an optional field needs an explicit `encodeNil`. FakeBackend and the AccountTests/ShellTests doubles don't conform to `MatchOutcomeStore`; the only conformers are SupabaseOutcomeQueries and three OnlineTests doubles. Tell dt-review not to create `STANDARDS.md` in lane runs.

## 2026-09-15 14:02 — dev-team-auto — final item 3: iPhone portrait except gameplay
- **Outcome:** BLOCKED — 2 attempts + a gate-closure pass — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high, dt-engineer sonnet (closure) — auto/final-a3 @ 4533ac0. Blocked on the iPad screenshot criterion alone; the three owed gates (delta pin of the initial mask default, the uiMask/delegate wiring scan, the pbxproj exact-literal) were closed at 4533ac0, each mutation-checked with a cp restore verified byte-identical, ShellTests 233/233, BUILD SUCCEEDED.
- **What happened:** The engineer built isGameplay, a plain OrientationPolicy, the OrientationLock adaptor, the ShellRootView onChange wiring and the pbxproj iPhone key. It is mutation-checked 7/7 and 6/6, and the build succeeded. Review found one Important (initial mask `.all`), fixed inline. QA failed the iPad landscape screenshot, and the deadline hit.
- **What worked:** Putting the plain policy in Shell/ so the existing ShellSrc symlink compiles it, with the UIKit shim and idiom read in App/. A scoped `#require` scan of ShellRootView.body. Running QA and review in parallel (review is read-only).
- **What failed:** The "height > width" screenshot criterion is invalid: `simctl io screenshot` always returns native portrait pixels. iPadOS 26.5 Simulator letterboxes a landscape-only app instead of rotating, so a cold-launch iPad screenshot cannot show landscape. The iPhone Home in portrait still uses its landscape layout and is cut off. A reinstall and reshoot under load took more than 2 minutes and was killed at the deadline.
- **Remember next run:** Judge orientation from image content, not pixel dimensions. Rotate the Simulator (Device > Rotate, which needs GUI access) or revise the iPad criterion. Budget more than 3 minutes for simctl install under sibling-build load. The portrait Home layout belongs to the comp-matching items.

## 2026-09-15 14:03 — dev-team-auto — final item 1: stop a fast drag flying home
- **Outcome:** DONE (orchestrator returned BLOCKED-out-of-time; session closed the two missing gates) — 1 attempt — caution: yes — team: dt-engineer opus/high, dt-qa opus/high, dt-review opus/high (+ orchestrator inline fix) — auto/final-a1 @ 8d9f610
- **What happened:** The lift hypothesis held: BrandTile.swift:52 draws `.selected` tiles tileLift (-8pt) higher and landing measured from the unlifted centre. The fix injects `lift` into drop/landed/landing and adds single-tile one-cell forgiveness. QA PASS at af117d7 (mutations M1–M5 isolated). Review found 1 Important: at 16pt the lift is half a cell, so a no-move release floored one row up. The orchestrator fixed it with an anchor-stays-home guard and a mutation-checked test. Session orchestrator then ran the delta review of af117d7..8d9f610 (clean; side effect: at any cell size a release whose lifted centre is just over the anchor's top edge stays home until the finger moves half a cell — symmetric with other directions) and ShellTests 227/227 on a quiet machine.
- **What worked:** QA ran its mutations on a copy of the tree in /tmp so they could not race the gates. Surrounding the target with 8 tiles (a `ring` fixture) kept the old refusal tests meaningful under forgiveness.
- **What failed:** ShellTests ran red at load average ~88 with 3 sibling items building (timeouts, bot suite); green 227/227 in 16s once quiet. The engineer took 25 min of a 48-min budget.
- **Remember next run:** The lift is half a cell at the 16pt minimum, so any lifted-centre drop math needs an anchor tie-break. Only the "cell below empty" half of the lift test pins the lift, because forgiveness absorbs the other half. Don't run ShellTests alongside xcodebuild under sibling load. With <50 min, run review first so a fix pass has time for a delta review.

## 2026-09-15 16:09 — dev-team-auto — final item 4: tighter margins on a portrait phone
- **Outcome:** DONE — 1 attempt — caution: no (ui: true) — team: dt-engineer opus/high, dt-qa opus/high — auto/final, commit e4ef400
- **What happened:** Added `Space.screenMarginPhone = 16` and a plain `ScreenMargin.value(horizontal:vertical:)` inside DesignTokens.swift; `ScreenPadding` now reads both size classes and calls it. StyleTests 34 passed (floor 31), xcodebuild BUILD SUCCEEDED, QA PASS first attempt.
- **What worked:** Making `Tests/StyleTests/Package.swift` a two-target package — a new `StyleKit` source target over per-file symlinks in `Tests/StyleTests/StyleSrc/` (DesignTokens.swift, BrandFonts.swift) — so StyleTests executes the real function instead of only scanning source. DesignTokens.swift and BrandFonts.swift typecheck for macOS 14 and `UserInterfaceSizeClass` is available there. Views still aren't compiled, so the `.screenPadding()` call site is covered by a scoped source scan; a call-site mutation killed it.
- **What failed:** none. `ScreenMargin` could not live in its own Style file: it needs DesignTokens, which the ShellTests Style target does not have, so it could not be symlinked into `Tests/ShellTests/StyleSrc`.
- **Remember next run:** `Tests/StyleTests` can now run real Style code — add a symlink under `Tests/StyleTests/StyleSrc/` for any Style file a future test needs, and remember new symlinks are untracked until explicitly `git add`ed. `ScreenMarginTests.screenPaddingCallsValue` matches one exact source string, so reformatting the `.padding(ScreenMargin.value(...))` line breaks the test with no behaviour change — update the expected string with it. StyleGallery has an "every key appears in the gallery" guardrail: adding a `Space` key requires a matching ruler row in StyleGallery.swift.

## 2026-09-15 16:45 — dev-team-auto — item 9 restyle Profile and Friends (comp 04–05, portrait)
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-ui sonnet/high, dt-qa opus/high — auto/final-e1, 0b9b5d7
- **What happened:** dt-ui restyled ProfileView and FriendsView to comp screens 04–05 and added `ProfileModel.winRatePercent` (whole percent, nil at zero played) plus its AccountTests case; dt-qa re-ran both mutations independently and passed every criterion first time. No rework, no escalation.
- **What worked:** Mandating in the builder prompt that the win-rate value live in the model rather than the view body — `swift test` does not compile views, so a view-body computation could not have satisfied the `done when:` case at all. Naming the builder's own reported smell (`doneButton(onBack:)` added to satisfy a literal source-string assertion) as an explicit scrutiny item in the QA prompt got it traced to the old `ScreenHeader` call path and cleared as behaviour-identical instead of being waved through.
- **What failed:** none. One build-gate hiccup inside the builder: `Button(action:)` needed `@escaping` on the new `doneButton(onBack:)` param — caught by its own xcodebuild, not a behaviour change.
- **Remember next run:** `SourceGuardrailTests` in AccountTests asserts literal source strings such as `"onBack: onBack"`, and it visibly bent production shape this item — a single-use `doneButton` helper exists only to satisfy the scan. Fix the scan (scope it to the declaration, assert `action: onBack`) before the next screen restyle, or every restyled header will inherit the same dead indirection. `SourceGuardrailTests.swift:168` ("No stat is worked out on the client") is scoped to the `stats` block only, so the new derived win rate slips past it — amend the comment so the next reader is not misled. Friends accepted-rows carry three fixed-width action buttons and are now tight at iPhone SE width; if a later item touches that row, wrap the actions or demote Block to a menu. Brand fonts are all `fixedSize`, so Dynamic Type findings in this app are app-wide and pre-existing — do not re-file them as per-item regressions. Concurrency held fine: two Swift builds at load ~16 were survivable, three were not earlier in the run.

## 2026-09-15 16:55 — dev-team-auto — item 10 How to Play pager + Solo setup portrait
- **Outcome:** DONE — 2 attempts — caution: no — team: dt-ui sonnet/high, dt-qa opus/high — branch `auto/final-e2`, commit 570c80c
- **What happened:** Attempt 1's dt-ui built and committed the work (`4e3e603`, `f9d7285`) but stranded without handing back, costing the attempt. On resume its work was found intact as commits, not as uncommitted edits, so nothing was rebuilt. I read the diffs myself and found the new pager tests asserted against the exported `HowToPlay.rules.count`; dt-qa rewrote them to written literals (`570c80c`), redid all five mutations, and passed 238/238 with BUILD SUCCEEDED.
- **What worked:** Reading the surviving diff and ruling on it myself before spawning QA caught the exported-constant defect that both the builder's own mutation sweep and its report missed — the builder had rationalized its format mutation breaking two tests instead of one. Handing QA the named defect plus the gate ownership avoided a second concurrent Swift build.
- **What failed:** A stranded builder cost a full attempt. QA's first mutation sweep used `--filter "How to play pager"` (the `@Suite` display name), which matched 0 tests and proved nothing — swift-testing filters on the TYPE name. Mutations (a) initial-page and (b) Next-advance are structurally non-isolable: `pageLabel` derives from `page`, and `next()` is the only forward transition, so every forward-dependent test rides on it.
- **Remember next run:** Filter swift-testing by type name (`--filter HowToPlayPagerTests`), never the `@Suite` display string — a 0-match filter exits green and silently proves nothing; assert the expected test count ran. When a plan says "assert written literals", check the builder's tests for the exported constant explicitly — it is the default thing a builder reaches for and it makes format mutations break two tests at once. A stranded subagent's work may already be COMMITTED — run `git log` before `git status`/`git diff` and never conclude work was lost from a clean tree. ShellTests Match/Results suites are wall-clock-timeout flaky above load ~10 and want an injected clock; gate only below load ~8. App-wide: `BrandFonts.swift` uses `.custom(_, fixedSize:)` so nothing scales with Dynamic Type — Dynamic Type defect families are currently unreachable, and fixing that deserves its own lane item.

## 2026-09-15 17:45 — dev-team-auto — item 5 Home rebuilt from comp screen 01
- **Outcome:** DONE — 2 attempts — caution: no — team: dt-ui (sonnet, high), dt-qa (opus, high) — auto/final, 17dffc2
- **What happened:** Rebuilt portrait Home as a single column with a six-slot `MenuLayout.actions` value ShellTests asserts against written literals; deleted the tagline, disabled Multiplayer with a "Coming soon" caption, moved Solo Practice into the quiet grid and dropped Join from Home. Attempt 1 passed every unit test but clipped the bottom grid row on a real device; attempt 2 fixed the cause and passed QA.
- **What worked:** The orchestrator opening the builder's screenshot itself and rejecting attempt 1 before spawning QA — it saved a full QA cycle on a state that was visibly red. QA re-running the mutation checks independently rather than trusting the builder's account, which correctly downgraded one "isolated" claim to non-isolable.
- **What failed:** Attempt 1 shipped a green 248-test suite over a visibly broken screen. `MenuLayout.estimatedContentHeight` models height arithmetically and cannot see font wrap, so "Solo Practice" wrapping to two lines overflowed the SE while every assertion stayed green.
- **Remember next run:** A portrait phone is NOT `verticalSizeClass == .compact` — any style that drops to a smaller font on `.compact` (e.g. `QuietButtonStyle`) silently keeps its large font in portrait and wraps. For this app's menu, force the font on the Text (closer to the leaf than the ButtonStyle's own `.font()`) and pass nil via an `@ViewBuilder` if/else so the other branch attaches no modifier — an unconditional `.font(nil)` shadows the style everywhere. Never accept an arithmetic layout unit test as proof a screen fits: it cannot see text wrap, so a screenshot is the only real check. No iPhone 13 mini runtime exists on this machine; iPhone SE (3rd gen) at 375×667 is the correct stricter substitute and 375pt genuinely is the narrowest supported width at deployment target 17.0. App-wide: `BrandFonts.swift:80,86` `.custom(_, fixedSize:)` means NOTHING scales with Dynamic Type, so every "scrolls under large Dynamic Type" guardrail in this lane is unexercisable until that changes — worth its own item.

## 2026-09-15 18:35 — dev-team-auto — item 6 One Play / Join a Friend screen
- **Outcome:** DONE — 1 attempt — caution: no — team: dt-ui sonnet/high, dt-qa opus/high — auto/final, 6449c10
- **What happened:** New `TwoPlayerView.swift` + `CodeTiles.swift` render both `.hostLobby` and `.join` from comp screen 03; `HostLobbyView.swift`/`JoinView.swift` deleted. The item assumed `showJoin()`/`playAFriend()` "already tear down the other model" — they did not; both had a `guard case .menu = route` and no teardown, so the builder relaxed each guard to accept the other lobby route and added `teardown()` + nil-out before building the next model.
- **What worked:** Requiring the QA agent to re-break all five mutations itself rather than trust the builder's account — it independently confirmed `hasLeft` is a real transport leave, not a `== nil` proxy. Charging QA explicitly with "make a real attempt to SEE the screen" produced a temporary debug launch-arg injection that rendered both modes on the simulator and was reverted cleanly; without that charge the item would have shipped with no visual evidence on a `ui: true` screen.
- **What failed:** QA's own Medium finding proposed `await model.work?.value` to make the short-code guardrail backend-observable; applying it and re-running the mutation showed the row assertion still never fires, because an invalid code matches no lobby so no row is written either way. Reverted. Cost ~10 minutes but replaced a plausible-sounding fix with a disproof.
- **Remember next run:** `Tests/ShellTests` flakes (~17 issues) on a cold/loaded machine and passes clean warm — re-run before calling a red a regression. `FakeBackend` has no call counter, so "never reaches the backend" guardrails in ShellTests are only provable via model-state proxies (`work != nil` means the network path was entered); a real counter would mean editing `Willagrams/Online/FakeBackend.swift`, outside the Shell lane. `HostLobbyLayout.swift` is now a confirmed orphan kept green by its own test — delete it with its test in a cleanup item. Reaching an auth-gated screen on the simulator is possible via a temporary `FakeBackend` + launch-arg injection in `WillagramsApp.swift`; it works and reverts cleanly, and is the only way to get eyes on these screens since nothing can tap the simulator.
