# Delta Review 3 — 69fe27c..0d45d32
**Date:** 2026-08-17
**Files Reviewed:** 3 source files (ResultsModel.swift, SoloPractice.swift, RematchTests.swift); delta-review-2.md ignored as a report artifact.
**Dimensions Swept:** Efficiency clean — Reliability 1 Important + 2 Minor — Scalability clean — Safety & Security clean — Fault Tolerance (same findings as Reliability, no separate count) — Data Integrity clean — Over-Engineering clean

**Verdict: safe.** The fix is correct for the path it names — a stale screen's Main Menu now declines identically forever, spends nothing, and leaks nothing. Suite green: 21/21 under `swift test --package-path Tests/ShellTests --filter "Rematch|ResultsModel"`. One Important remains: the same regression is still reachable through `rematch()`, which spends `teardown` on a press that declined.

## Answers

1. **Does not-spending on decline leak? No.** The production teardown (`SoloPractice.swift:146-153`) captures `[weak self]` on `SoloPractice` and `generation` (an `Int` by value). It captures no `SoloMatch` and no `MatchSession` — the match is reached only through `self.end()`. A stale screen that never spends its closure therefore retains a weak box and an integer, nothing more. `tenRematchesStrandNothing` (RematchTests.swift:114) and `mainMenuReleasesTheSession` (ResultsModelTests.swift:196) are unaffected and still green, and the new test itself asserts the *live* match stays alive rather than that a dead one is freed — no assertion was traded away. See Minor #2 for the one gap: the doc contract at ResultsModel.swift:58-61 claims the closure holds the match strongly, and no test covers a strong-capturing teardown that declines.
2. **Does the live path still spend exactly once? Yes.** A live teardown returns `true`, so `teardown`/`startRematch` are nil'd and `returnToMenu()` runs. The second press finds `teardown == nil`, does not call it, and `mainMenuTearsTheMatchDown` (ResultsModelTests.swift:188-189) still asserts `leaveCalls == 1` after that second press — the assertion means exactly what it meant. `rematchIsEnabledOnlyWhenWired` (ResultsModelTests.swift:328-331) still proves Main Menu spends `startRematch` on the navigating path. Both were always the tearing-down leg, so the reorder cannot have hollowed them out. Caveat in Minor #1: the second press still calls `returnToMenu()`.
3. **`guard let self else { return true }` — correct.** A deallocated `SoloPractice` released its `match` with it; `SoloMatch.deinit` (SoloMatch.swift:147) cancels the peer pump and `MatchSession` has its own `deinit`. There is no live match whose route needs holding back, so `true` is truthful and, post-reorder, it is also the only value that keeps the button from becoming permanently inert with no other way off the end screen. `false` would now be sticky forever, which is strictly worse than the pre-delta behavior.
4. **`rematch()` is affected — see the Important below.** The two exits are now inconsistent, and the inconsistency is reachable, not cosmetic.
5. **The new test is not vacuous.** `RematchTests.swift:412-419`: the loop body calls `stale.mainMenu()` on every iteration (the press is inside the loop, not hoisted), and each of the four assertions has teeth. `shell.route == liveRoute` is guarded against a vacuous `liveRoute` by `shell.route != .menu` on the next line. `practice.match === new` is identity, not optionality. `new.session.isMatchOver == false` is real: `MatchSession.leave()` sets `peerPresence = .gone` (MatchSession.swift:459) and `isMatchOver` is `isFinished || peerPresence == .gone` (MatchSession.swift:159), so a teardown that actually ran flips it. The positive leg at RematchTests.swift:422-426 builds a fresh screen at the current generation and asserts both `.menu` and `practice.match == nil`, so an always-declining guard fails there. Mutation result reported (fails at presses 2 and 3) matches the code.
6. **FENCE: clean.** Source changes touch only `Willagrams/Shell/ResultsModel.swift`, `Willagrams/Shell/SoloPractice.swift`, `Tests/ShellTests/Cases/RematchTests.swift`.

## Findings

### Important
- `Willagrams/Shell/ResultsModel.swift:178-179` — Reliability / Fault Tolerance — `rematch()` still runs `_ = teardown?()` then `teardown = nil` unconditionally, so a declined press spends the closure there. Sequence: on a stale screen press Rematch (both closures are generation-guarded, so nothing is rebuilt and nothing is torn down, but `teardown` is dropped), then press Main Menu — `teardown` is now `nil`, `?? true` fires, and `shell.returnToMenu()` yanks the route to the menu while the replacing match is still live. That is the exact regression this delta closed, entered through the other door. Fix: nil the closure only when it tore down — `if teardown?() ?? true { teardown = nil }` — which also stops `rematch()` returning `true` for a press that did nothing. (Related to the deferred "drop `teardown?()` from `rematch()`" refactor, but the cross-path hole is a live defect in the current code, not a cleanup.)

### Minor
- `Willagrams/Shell/ResultsModel.swift:204-207` — Reliability — the success path still turns the screen into a nil-teardown screen, the state the new comment identifies as "the one state that navigates unconditionally". A second press on an already-spent live screen re-runs `shell.returnToMenu()`; if a new match has started since, the route is moved with nothing torn down. Narrow (the spent screen must outlive its own navigation and a fresh start), and unchanged from pre-delta behavior, but it is the same asymmetry the decline path just fixed. Fix: one `private var spent = false` set on the navigating path and checked first, which closes this and the Important above together.
- `Willagrams/Shell/ResultsModel.swift:58-61` — Reliability — the doc still states the closure "is expected to capture the match strongly", and a declined press now holds that closure for the screen's whole lifetime. Today's only production construction site (`SoloPractice.swift:146`) captures `[weak self]`, so nothing is retained; a future site that honors the doc literally would pin a replaced match to every stale screen, and no test covers strong-capture-plus-decline (`mainMenuReleasesTheSession` only exercises a teardown that returns `true`). Fix: amend the doc to say the closure must not outlive-retain the match, or add one release assertion on a declining strong-capture teardown.

## STANDARDS.md Updates
none — scoped delta re-review, no new project-wide convention observed.
