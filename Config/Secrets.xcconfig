// Committed defaults. Real values live in Secrets.local.xcconfig (gitignored),
// written by the anon-key wizard; the optional include means a fresh clone
// builds with an empty key instead of failing on a missing file.
SUPABASE_ANON_KEY =
#include? "Secrets.local.xcconfig"
